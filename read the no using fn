#include<stdio.h>
int print(int n)
{ int a[50],i;
printf(" the eles are");
  for(i=0;i<n;i++)
  {
   scanf("%d",&a[i]);
   
   }
   return a[i];
}
 int main()
  {
      int n;
  printf("enter the no of elements:\n");
  scanf("%d",&n); 
  print(n);
  }
